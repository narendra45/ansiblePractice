- name: conditions
  hosts: web
  become: yes
  tasks:
    - name: Check user exists or not
      ansible.builtin.command: id expense
    
    - name:
      ansible.builtin.debug:
        msg: "Hello World"